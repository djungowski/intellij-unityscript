class Application {
    /**
     * The absolute path to the web player data file (Read Only).
     */
    static get absoluteURL() {}

    /**
     * The absolute path to the web player data file (Read Only).
     */
    static set absoluteURL(value) {}

    /**
     * Priority of background loading thread.
     */
    static get backgroundLoadingPriority() {}

    /**
     * Priority of background loading thread.
     */
    static set backgroundLoadingPriority(value) {}

    /**
     * Returns application bundle identifier at runtime.
     */
    static get bundleIdentifier() {}

    /**
     * Returns application bundle identifier at runtime.
     */
    static set bundleIdentifier(value) {}

    /**
     * A unique cloud project identifier. It is unique for every project (Read Only).
     */
    static get cloudProjectId() {}

    /**
     * A unique cloud project identifier. It is unique for every project (Read Only).
     */
    static set cloudProjectId(value) {}

    /**
     * Return application company name (Read Only).
     */
    static get companyName() {}

    /**
     * Return application company name (Read Only).
     */
    static set companyName(value) {}

    /**
     * Contains the path to the game data folder (Read Only).
     */
    static get dataPath() {}

    /**
     * Contains the path to the game data folder (Read Only).
     */
    static set dataPath(value) {}

    /**
     * Returns false if application is altered in any way after it was built.
     */
    static get genuine() {}

    /**
     * Returns false if application is altered in any way after it was built.
     */
    static set genuine(value) {}

    /**
     * Returns true if application integrity can be confirmed.
     */
    static get genuineCheckAvailable() {}

    /**
     * Returns true if application integrity can be confirmed.
     */
    static set genuineCheckAvailable(value) {}

    /**
     * Returns the name of the store or package that installed the application (Read Only).
     */
    static get installerName() {}

    /**
     * Returns the name of the store or package that installed the application (Read Only).
     */
    static set installerName(value) {}

    /**
     * Returns application install mode (Read Only).
     */
    static get installMode() {}

    /**
     * Returns application install mode (Read Only).
     */
    static set installMode(value) {}

    /**
     * Returns the type of Internet reachability currently possible on the device.
     */
    static get internetReachability() {}

    /**
     * Returns the type of Internet reachability currently possible on the device.
     */
    static set internetReachability(value) {}

    /**
     * Is the current Runtime platform a known console platform.
     */
    static get isConsolePlatform() {}

    /**
     * Is the current Runtime platform a known console platform.
     */
    static set isConsolePlatform(value) {}

    /**
     * Are we running inside the Unity editor? (Read Only)
     */
    static get isEditor() {}

    /**
     * Are we running inside the Unity editor? (Read Only)
     */
    static set isEditor(value) {}

    /**
     * Is the current Runtime platform a known mobile platform.
     */
    static get isMobilePlatform() {}

    /**
     * Is the current Runtime platform a known mobile platform.
     */
    static set isMobilePlatform(value) {}

    /**
     * Returns true when in any kind of player (Read Only).
     */
    static get isPlaying() {}

    /**
     * Returns true when in any kind of player (Read Only).
     */
    static set isPlaying(value) {}

    /**
     * Checks whether splash screen is being shown.
     */
    static get isShowingSplashScreen() {}

    /**
     * Checks whether splash screen is being shown.
     */
    static set isShowingSplashScreen(value) {}

    /**
     * Are we running inside a web player? (Read Only)
     */
    static get isWebPlayer() {}

    /**
     * Are we running inside a web player? (Read Only)
     */
    static set isWebPlayer(value) {}

    /**
     * Contains the path to a persistent data directory (Read Only).
     */
    static get persistentDataPath() {}

    /**
     * Contains the path to a persistent data directory (Read Only).
     */
    static set persistentDataPath(value) {}

    /**
     * Returns the platform the game is running on (Read Only).
     */
    static get platform() {}

    /**
     * Returns the platform the game is running on (Read Only).
     */
    static set platform(value) {}

    /**
     * Returns application product name (Read Only).
     */
    static get productName() {}

    /**
     * Returns application product name (Read Only).
     */
    static set productName(value) {}

    /**
     * Should the player be running when the application is in the background?
     */
    static get runInBackground() {}

    /**
     * Should the player be running when the application is in the background?
     */
    static set runInBackground(value) {}

    /**
     * Returns application running in sandbox (Read Only).
     */
    static get sandboxType() {}

    /**
     * Returns application running in sandbox (Read Only).
     */
    static set sandboxType(value) {}

    /**
     * The path to the web player data file relative to the html file (Read Only).
     */
    static get srcValue() {}

    /**
     * The path to the web player data file relative to the html file (Read Only).
     */
    static set srcValue(value) {}

    /**
     * How many bytes have we downloaded from the main unity web stream (Read Only).
     */
    static get streamedBytes() {}

    /**
     * How many bytes have we downloaded from the main unity web stream (Read Only).
     */
    static set streamedBytes(value) {}

    /**
     * Contains the path to the StreamingAssets folder (Read Only).
     */
    static get streamingAssetsPath() {}

    /**
     * Contains the path to the StreamingAssets folder (Read Only).
     */
    static set streamingAssetsPath(value) {}

    /**
     * The language the user&#039;s operating system is running in.
     */
    static get systemLanguage() {}

    /**
     * The language the user&#039;s operating system is running in.
     */
    static set systemLanguage(value) {}

    /**
     * Instructs game to try to render at a specified frame rate.
     */
    static get targetFrameRate() {}

    /**
     * Instructs game to try to render at a specified frame rate.
     */
    static set targetFrameRate(value) {}

    /**
     * Contains the path to a temporary data / cache directory (Read Only).
     */
    static get temporaryCachePath() {}

    /**
     * Contains the path to a temporary data / cache directory (Read Only).
     */
    static set temporaryCachePath(value) {}

    /**
     * The version of the Unity runtime used to play the content.
     */
    static get unityVersion() {}

    /**
     * The version of the Unity runtime used to play the content.
     */
    static set unityVersion(value) {}

    /**
     * Returns application version number  (Read Only).
     */
    static get version() {}

    /**
     * Returns application version number  (Read Only).
     */
    static set version(value) {}




}