class QueueMode {


    /**
     * Will start playing after all other animations have stopped playing.
     */
    get CompleteOthers() {}

    /**
     * Will start playing after all other animations have stopped playing.
     */
    set CompleteOthers(value) {}

    /**
     * Starts playing immediately. This can be used if you just want to quickly create a duplicate animation.
     */
    get PlayNow() {}

    /**
     * Starts playing immediately. This can be used if you just want to quickly create a duplicate animation.
     */
    set PlayNow(value) {}


}