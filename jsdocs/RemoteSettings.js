class RemoteSettings {

    /**
     * Returns the value corresponding to key in the remote settings if it exists.
     */
    static GetBool() {}

    /**
     * Returns the value corresponding to key in the remote settings if it exists.
     */
    static GetFloat() {}

    /**
     * Returns the value corresponding to key in the remote settings if it exists.
     */
    static GetInt() {}

    /**
     * Returns the value corresponding to key in the remote settings if it exists.
     */
    static GetString() {}

    /**
     * Returns true if key exists in the remote settings.
     */
    static HasKey() {}



}