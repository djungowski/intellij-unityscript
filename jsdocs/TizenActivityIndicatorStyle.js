class TizenActivityIndicatorStyle {


    /**
     * Sets your game to not show any indicator while loading.
     */
    get DontShow() {}

    /**
     * Sets your game to not show any indicator while loading.
     */
    set DontShow(value) {}

    /**
     * The loading indicator size is large and rotates clockwise (progress_large).
     */
    get Large() {}

    /**
     * The loading indicator size is large and rotates clockwise (progress_large).
     */
    set Large(value) {}

    /**
     * The loading indicator size is large and rotates counterclockwise (progress_large and inverted).
     */
    get InversedLarge() {}

    /**
     * The loading indicator size is large and rotates counterclockwise (progress_large and inverted).
     */
    set InversedLarge(value) {}

    /**
     * The loading indicator size is small and rotates clockwise (process_small).
     */
    get Small() {}

    /**
     * The loading indicator size is small and rotates clockwise (process_small).
     */
    set Small(value) {}

    /**
     * The loading indicator size is small and rotates counterclockwise (process_small and inverted).
     */
    get InversedSmall() {}

    /**
     * The loading indicator size is small and rotates counterclockwise (process_small and inverted).
     */
    set InversedSmall(value) {}


}