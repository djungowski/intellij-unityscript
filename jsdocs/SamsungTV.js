class SamsungTV {
    /**
     * Returns true if there is an air mouse available.
     */
    static get airMouseConnected() {}

    /**
     * Returns true if there is an air mouse available.
     */
    static set airMouseConnected(value) {}

    /**
     * Changes the type of input the gamepad produces.
     */
    static get gamePadMode() {}

    /**
     * Changes the type of input the gamepad produces.
     */
    static set gamePadMode(value) {}

    /**
     * Changes the type of input the gesture camera produces.
     */
    static get gestureMode() {}

    /**
     * Changes the type of input the gesture camera produces.
     */
    static set gestureMode(value) {}

    /**
     * Returns true if the camera sees a hand.
     */
    static get gestureWorking() {}

    /**
     * Returns true if the camera sees a hand.
     */
    static set gestureWorking(value) {}

    /**
     * The type of input the remote&#039;s touch pad produces.
     */
    static get touchPadMode() {}

    /**
     * The type of input the remote&#039;s touch pad produces.
     */
    static set touchPadMode(value) {}


    /**
     * Set the system language that is returned by Application.SystemLanguage.
     */
    static SetSystemLanguage() {}



}