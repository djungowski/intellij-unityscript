class Tools {
    /**
     * The tool that is currently selected for the Scene View.
     */
    static get current() {}

    /**
     * The tool that is currently selected for the Scene View.
     */
    static set current(value) {}

    /**
     * The position of the tool handle in world space.
     */
    static get handlePosition() {}

    /**
     * The position of the tool handle in world space.
     */
    static set handlePosition(value) {}

    /**
     * The rectangle used for the rect tool.
     */
    static get handleRect() {}

    /**
     * The rectangle used for the rect tool.
     */
    static set handleRect(value) {}

    /**
     * The rotation of the rect tool handle in world space.
     */
    static get handleRectRotation() {}

    /**
     * The rotation of the rect tool handle in world space.
     */
    static set handleRectRotation(value) {}

    /**
     * The rotation of the tool handle in world space.
     */
    static get handleRotation() {}

    /**
     * The rotation of the tool handle in world space.
     */
    static set handleRotation(value) {}

    /**
     * Hides the Tools(Move, Rotate, Resize) on the Scene view.
     */
    static get hidden() {}

    /**
     * Hides the Tools(Move, Rotate, Resize) on the Scene view.
     */
    static set hidden(value) {}

    /**
     * Are we in Center or Pivot mode.
     */
    static get pivotMode() {}

    /**
     * Are we in Center or Pivot mode.
     */
    static set pivotMode(value) {}

    /**
     * What&#039;s the rotation of the tool handle.
     */
    static get pivotRotation() {}

    /**
     * What&#039;s the rotation of the tool handle.
     */
    static set pivotRotation(value) {}

    /**
     * Is the rect handle in blueprint mode?
     */
    static get rectBlueprintMode() {}

    /**
     * Is the rect handle in blueprint mode?
     */
    static set rectBlueprintMode(value) {}

    /**
     * The option that is currently active for the View tool in the Scene view.
     */
    static get viewTool() {}

    /**
     * The option that is currently active for the View tool in the Scene view.
     */
    static set viewTool(value) {}

    /**
     * Which layers are visible in the scene view.
     */
    static get visibleLayers() {}

    /**
     * Which layers are visible in the scene view.
     */
    static set visibleLayers(value) {}


    /**
     * Returns the instance id of the object.
     */
    static GetInstanceID() {}

    /**
     * Returns the name of the game object.
     */
    static ToString() {}


    /**
     * Should the object be hidden, saved with the scene or modifiable by the user?
     */
    get hideFlags() {}

    /**
     * Should the object be hidden, saved with the scene or modifiable by the user?
     */
    set hideFlags(value) {}

    /**
     * The name of the object.
     */
    get name() {}

    /**
     * The name of the object.
     */
    set name(value) {}


    /**
     * Returns the instance id of the object.
     */
    GetInstanceID() {}

    /**
     * Returns the name of the game object.
     */
    ToString() {}

}