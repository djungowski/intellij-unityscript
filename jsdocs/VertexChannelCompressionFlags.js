class VertexChannelCompressionFlags {


    /**
     * Position.
     */
    get kPosition() {}

    /**
     * Position.
     */
    set kPosition(value) {}

    /**
     * Vertex normal.
     */
    get kNormal() {}

    /**
     * Vertex normal.
     */
    set kNormal(value) {}

    /**
     * Vertex color.
     */
    get kColor() {}

    /**
     * Vertex color.
     */
    set kColor(value) {}

    /**
     * Texture coordinate channel 0. Usually used for Albedo texture.
     */
    get kUV0() {}

    /**
     * Texture coordinate channel 0. Usually used for Albedo texture.
     */
    set kUV0(value) {}

    /**
     * Texture coordinate channel 1. Usually used for baked lightmap.
     */
    get kUV1() {}

    /**
     * Texture coordinate channel 1. Usually used for baked lightmap.
     */
    set kUV1(value) {}

    /**
     * Texture coordinate channel 2. Usually used for realtime GI.
     */
    get kUV2() {}

    /**
     * Texture coordinate channel 2. Usually used for realtime GI.
     */
    set kUV2(value) {}

    /**
     * Texture coordinate channel 3.
     */
    get kUV3() {}

    /**
     * Texture coordinate channel 3.
     */
    set kUV3(value) {}

    /**
     * Tangent.
     */
    get kTangent() {}

    /**
     * Tangent.
     */
    set kTangent(value) {}


}