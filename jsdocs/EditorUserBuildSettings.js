class EditorUserBuildSettings {
    /**
     * The currently active build target.
     */
    static get activeBuildTarget() {}

    /**
     * The currently active build target.
     */
    static set activeBuildTarget(value) {}

    /**
     * Triggered in response to SwitchActiveBuildTarget.
     */
    static get activeBuildTargetChanged() {}

    /**
     * Triggered in response to SwitchActiveBuildTarget.
     */
    static set activeBuildTargetChanged(value) {}

    /**
     * DEFINE directives for the compiler.
     */
    static get activeScriptCompilationDefines() {}

    /**
     * DEFINE directives for the compiler.
     */
    static set activeScriptCompilationDefines(value) {}

    /**
     * Enable source-level debuggers to connect.
     */
    static get allowDebugging() {}

    /**
     * Enable source-level debuggers to connect.
     */
    static set allowDebugging(value) {}

    /**
     * Android platform options.
     */
    static get androidBuildSubtarget() {}

    /**
     * Android platform options.
     */
    static set androidBuildSubtarget(value) {}

    /**
     * Set which build system to use for building the Android package.
     */
    static get androidBuildSystem() {}

    /**
     * Set which build system to use for building the Android package.
     */
    static set androidBuildSystem(value) {}

    /**
     * Is build script only enabled.
     */
    static get buildScriptsOnly() {}

    /**
     * Is build script only enabled.
     */
    static set buildScriptsOnly(value) {}

    /**
     * Build data compressed with PSArc.
     */
    static get compressWithPsArc() {}

    /**
     * Build data compressed with PSArc.
     */
    static set compressWithPsArc(value) {}

    /**
     * Start the player with a connection to the profiler.
     */
    static get connectProfiler() {}

    /**
     * Start the player with a connection to the profiler.
     */
    static set connectProfiler(value) {}

    /**
     * Enables a development build.
     */
    static get development() {}

    /**
     * Enables a development build.
     */
    static set development(value) {}

    /**
     * Enables a Linux headless build.
     */
    static get enableHeadlessMode() {}

    /**
     * Enables a Linux headless build.
     */
    static set enableHeadlessMode(value) {}

    /**
     * Are divide by zero&#039;s actively checked?
     */
    static get explicitDivideByZeroChecks() {}

    /**
     * Are divide by zero&#039;s actively checked?
     */
    static set explicitDivideByZeroChecks(value) {}

    /**
     * Are null references actively checked?
     */
    static get explicitNullChecks() {}

    /**
     * Are null references actively checked?
     */
    static set explicitNullChecks(value) {}

    /**
     * Export Android Project for use with Android Studio/Gradle or Eclipse/ADT.
     */
    static get exportAsGoogleAndroidProject() {}

    /**
     * Export Android Project for use with Android Studio/Gradle or Eclipse/ADT.
     */
    static set exportAsGoogleAndroidProject(value) {}

    /**
     * Force installation of package, even if error.
     */
    static get forceInstallation() {}

    /**
     * Force installation of package, even if error.
     */
    static set forceInstallation(value) {}

    /**
     * Force full optimizations for script complilation in Development builds.
     */
    static get forceOptimizeScriptCompilation() {}

    /**
     * Force full optimizations for script complilation in Development builds.
     */
    static set forceOptimizeScriptCompilation(value) {}

    /**
     * Place the built player in the build folder.
     */
    static get installInBuildFolder() {}

    /**
     * Place the built player in the build folder.
     */
    static set installInBuildFolder(value) {}

    /**
     * Scheme with which the project will be run in Xcode.
     */
    static get iOSBuildConfigType() {}

    /**
     * Scheme with which the project will be run in Xcode.
     */
    static set iOSBuildConfigType(value) {}

    /**
     * Create a .cia &quot;download image&quot; for deploying to test kits (3DS).
     */
    static get n3dsCreateCIAFile() {}

    /**
     * Create a .cia &quot;download image&quot; for deploying to test kits (3DS).
     */
    static set n3dsCreateCIAFile(value) {}

    /**
     * Build submission materials.
     */
    static get needSubmissionMaterials() {}

    /**
     * Build submission materials.
     */
    static set needSubmissionMaterials(value) {}

    /**
     * PS4 Build Subtarget.
     */
    static get ps4BuildSubtarget() {}

    /**
     * PS4 Build Subtarget.
     */
    static set ps4BuildSubtarget(value) {}

    /**
     * Specifies which version of PS4 hardware to target.
     */
    static get ps4HardwareTarget() {}

    /**
     * Specifies which version of PS4 hardware to target.
     */
    static set ps4HardwareTarget(value) {}

    /**
     * PSM Build Subtarget.
     */
    static get psmBuildSubtarget() {}

    /**
     * PSM Build Subtarget.
     */
    static set psmBuildSubtarget(value) {}

    /**
     * PS Vita Build subtarget.
     */
    static get psp2BuildSubtarget() {}

    /**
     * PS Vita Build subtarget.
     */
    static set psp2BuildSubtarget(value) {}

    /**
     * The currently selected build target group.
     */
    static get selectedBuildTargetGroup() {}

    /**
     * The currently selected build target group.
     */
    static set selectedBuildTargetGroup(value) {}

    /**
     * The currently selected target for a standalone build.
     */
    static get selectedStandaloneTarget() {}

    /**
     * The currently selected target for a standalone build.
     */
    static set selectedStandaloneTarget(value) {}

    /**
     * When building an Xbox One Streaming Install package (makepkg.exe) The layout generation code in Unity will assign each scene and associated assets to individual chunks. Unity will mark scene 0 as being part of the launch range, IE the set of chunks required to launch the game, you may include additional scenes in this launch range if you desire, this specifies a range of scenes (starting at 0) to be included in the launch set. 
     */
    static get streamingInstallLaunchRange() {}

    /**
     * When building an Xbox One Streaming Install package (makepkg.exe) The layout generation code in Unity will assign each scene and associated assets to individual chunks. Unity will mark scene 0 as being part of the launch range, IE the set of chunks required to launch the game, you may include additional scenes in this launch range if you desire, this specifies a range of scenes (starting at 0) to be included in the launch set. 
     */
    static set streamingInstallLaunchRange(value) {}

    /**
     * Symlink runtime libraries with an iOS Xcode project.
     */
    static get symlinkLibraries() {}

    /**
     * Symlink runtime libraries with an iOS Xcode project.
     */
    static set symlinkLibraries(value) {}

    /**
     * The texture compression type to be used when building.
     */
    static get tizenBuildSubtarget() {}

    /**
     * The texture compression type to be used when building.
     */
    static set tizenBuildSubtarget(value) {}

    /**
     * Use prebuilt JavaScript version of Unity engine.
     */
    static get webGLUsePreBuiltUnityEngine() {}

    /**
     * Use prebuilt JavaScript version of Unity engine.
     */
    static set webGLUsePreBuiltUnityEngine(value) {}

    /**
     * Build the webplayer along with the UnityObject.js file (so it doesn&#039;t need to be downloaded).
     */
    static get webPlayerOfflineDeployment() {}

    /**
     * Build the webplayer along with the UnityObject.js file (so it doesn&#039;t need to be downloaded).
     */
    static set webPlayerOfflineDeployment(value) {}

    /**
     * Select the streaming option for a webplayer build.
     */
    static get webPlayerStreamed() {}

    /**
     * Select the streaming option for a webplayer build.
     */
    static set webPlayerStreamed(value) {}

    /**
     * Boot mode of a devkit.
     */
    static get wiiUBootMode() {}

    /**
     * Boot mode of a devkit.
     */
    static set wiiUBootMode(value) {}

    /**
     * Wii U player debug level.
     */
    static get wiiUBuildDebugLevel() {}

    /**
     * Wii U player debug level.
     */
    static set wiiUBuildDebugLevel(value) {}

    /**
     * Built player postprocessing options.
     */
    static get wiiuBuildOutput() {}

    /**
     * Built player postprocessing options.
     */
    static set wiiuBuildOutput(value) {}

    /**
     * Enable network API.
     */
    static get wiiUEnableNetAPI() {}

    /**
     * Enable network API.
     */
    static set wiiUEnableNetAPI(value) {}

    /**
     * Generate and reference C# projects from your main solution.
     */
    static get wsaGenerateReferenceProjects() {}

    /**
     * Generate and reference C# projects from your main solution.
     */
    static set wsaGenerateReferenceProjects(value) {}

    /**
     * Target Windows SDK.
     */
    static get wsaSDK() {}

    /**
     * Target Windows SDK.
     */
    static set wsaSDK(value) {}

    /**
     * Sets and gets target device type for the application to run on when building to Windows Store platform.
     */
    static get wsaSubtarget() {}

    /**
     * Sets and gets target device type for the application to run on when building to Windows Store platform.
     */
    static set wsaSubtarget(value) {}

    /**
     * Xbox Build subtarget.
     */
    static get xboxBuildSubtarget() {}

    /**
     * Xbox Build subtarget.
     */
    static set xboxBuildSubtarget(value) {}

    /**
     * The currently selected Xbox One Deploy Method.
     */
    static get xboxOneDeployMethod() {}

    /**
     * The currently selected Xbox One Deploy Method.
     */
    static set xboxOneDeployMethod(value) {}

    /**
     * Network shared folder path e.g. MYCOMPUTER\SHAREDFOLDER\.
     */
    static get xboxOneNetworkSharePath() {}

    /**
     * Network shared folder path e.g. MYCOMPUTER\SHAREDFOLDER\.
     */
    static set xboxOneNetworkSharePath(value) {}

    /**
     * Windows account username associated with PC share folder.
     */
    static get xboxOneUsername() {}

    /**
     * Windows account username associated with PC share folder.
     */
    static set xboxOneUsername(value) {}




}