class UILineInfo {


    /**
     * Height of the line.
     */
    get height() {}

    /**
     * Height of the line.
     */
    set height(value) {}

    /**
     * Index of the first character in the line.
     */
    get startCharIdx() {}

    /**
     * Index of the first character in the line.
     */
    set startCharIdx(value) {}

    /**
     * The upper Y position of the line in pixels. This is used for text annotation such as the caret and selection box in the InputField.
     */
    get topY() {}

    /**
     * The upper Y position of the line in pixels. This is used for text annotation such as the caret and selection box in the InputField.
     */
    set topY(value) {}


}