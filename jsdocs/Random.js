class Random {
    /**
     * Returns a random point inside a circle with radius 1 (Read Only).
     */
    static get insideUnitCircle() {}

    /**
     * Returns a random point inside a circle with radius 1 (Read Only).
     */
    static set insideUnitCircle(value) {}

    /**
     * Returns a random point inside a sphere with radius 1 (Read Only).
     */
    static get insideUnitSphere() {}

    /**
     * Returns a random point inside a sphere with radius 1 (Read Only).
     */
    static set insideUnitSphere(value) {}

    /**
     * Returns a random point on the surface of a sphere with radius 1 (Read Only).
     */
    static get onUnitSphere() {}

    /**
     * Returns a random point on the surface of a sphere with radius 1 (Read Only).
     */
    static set onUnitSphere(value) {}

    /**
     * Returns a random rotation (Read Only).
     */
    static get rotation() {}

    /**
     * Returns a random rotation (Read Only).
     */
    static set rotation(value) {}

    /**
     * Returns a random rotation with uniform distribution (Read Only).
     */
    static get rotationUniform() {}

    /**
     * Returns a random rotation with uniform distribution (Read Only).
     */
    static set rotationUniform(value) {}

    /**
     * Gets/Sets the full internal state of the random number generator.
     */
    static get state() {}

    /**
     * Gets/Sets the full internal state of the random number generator.
     */
    static set state(value) {}

    /**
     * Returns a random number between 0.0 [inclusive] and 1.0 [inclusive] (Read Only).
     */
    static get value() {}

    /**
     * Returns a random number between 0.0 [inclusive] and 1.0 [inclusive] (Read Only).
     */
    static set value(value) {}


    /**
     * Generates a random color from HSV and alpha ranges.
     */
    static ColorHSV() {}

    /**
     * Initializes the random number generator state with a seed.
     */
    static InitState() {}

    /**
     * Returns a random float number between and min [inclusive] and max [inclusive] (Read Only).
     */
    static Range() {}



}