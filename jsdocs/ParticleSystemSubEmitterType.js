class ParticleSystemSubEmitterType {


    /**
     * Spawn new particles when particles from the parent system are born.
     */
    get Birth() {}

    /**
     * Spawn new particles when particles from the parent system are born.
     */
    set Birth(value) {}

    /**
     * Spawn new particles when particles from the parent system collide with something.
     */
    get Collision() {}

    /**
     * Spawn new particles when particles from the parent system collide with something.
     */
    set Collision(value) {}

    /**
     * Spawn new particles when particles from the parent system die.
     */
    get Death() {}

    /**
     * Spawn new particles when particles from the parent system die.
     */
    set Death(value) {}


}